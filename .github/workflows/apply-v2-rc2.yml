name: EZ Enablement V2 RC2 Handoff (inactive)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RC2 handoff completed. This bootstrap workflow is inactive."
